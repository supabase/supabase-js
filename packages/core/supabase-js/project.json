{
  "name": "@supabase/supabase-js",
  "targets": {
    "test": {
      "inputs": ["testing", "^production"]
    },
    "test:unit": {
      "inputs": ["testing", "^production"]
    },
    "test:coverage": {
      "inputs": ["testing", "^production"]
    },
    "test:integration": {
      "inputs": ["testing", "^production"]
    },
    "test:all": {
      "inputs": ["testing", "^production"]
    },
    "test:esm": {
      "inputs": ["testing", "^production"],
      "dependsOn": ["build", "^build"],
      "cache": true
    },
    "test:cjs": {
      "inputs": ["testing", "^production"],
      "dependsOn": ["build", "^build"],
      "cache": true
    },
    "test:exports": {
      "inputs": ["testing", "^production"],
      "dependsOn": ["build", "^build"],
      "cache": true
    }
  }
}
