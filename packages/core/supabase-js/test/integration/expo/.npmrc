# See packages/core/supabase-js/test/integration/next/.npmrc for rationale.
# This sub-app installs supabase-js from pkg.pr.new URLs whose published
# manifest references sibling packages by URL; the root workspace's
# blockExoticSubdeps rule would reject those transitive URLs.
block-exotic-subdeps=false
