/**
 * Default HTTP headers for all requests
 * Includes client identification for analytics and debugging
 */
export const DEFAULT_HEADERS = {
  'X-Client-Info': `storage-vectors-js/0.0.1`,
  'Content-Type': 'application/json'
}
