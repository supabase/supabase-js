# Refuse to install package versions younger than this.
# Defense against fast-detection supply-chain attacks (TanStack, tj-actions).
# Requires npm >= 11.5. Migrates to pnpm-workspace.yaml in a later PR
# (pnpm 10+ reads the same value as `minimumReleaseAge`).
minimum-release-age=10080
